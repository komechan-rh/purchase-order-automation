{
  "name": "purchase-orderautomation-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc -p tsconfig.json && cp appsscript.json dist/appsscript.json",
    "push": "pnpm run build && clasp push --force",
    "pull": "clasp pull"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.96",
    "typescript": "^5.6.2"
  }
}
